# Pinned snapshot for environments without `uv` / `pip install -e .`.
# Authoritative dependency list lives in pyproject.toml — keep this file
# in sync if you change pyproject.

numpy>=1.26,<2.3
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
librosa>=0.10.1
soundfile>=0.12
torch>=2.1
matplotlib>=3.8
seaborn>=0.13
tqdm>=4.66
pyyaml>=6.0
